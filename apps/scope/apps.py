from django.apps import AppConfig


class ScopeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.scope'
    verbose_name = 'Scope & Requirements Management'
